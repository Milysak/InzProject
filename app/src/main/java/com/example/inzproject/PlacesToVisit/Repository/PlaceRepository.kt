package com.example.inzproject.PlacesToVisit.Repository

import javax.inject.Inject

class PlaceRepository @Inject constructor(private val api:PlaceApi){

     //suspend fun getPlaces(loc:String,type:String)=api.getPlaces()
}